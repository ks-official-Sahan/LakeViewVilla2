"use client";

import type { HeroComponentProps } from "../types";

/** Phase 4 — instanced koi/tilapia with swim phase attribute. */
export function Fish(_props: HeroComponentProps = {}) {
  return null;
}
