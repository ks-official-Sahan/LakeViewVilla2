"use client";

import type { HeroComponentProps } from "../types";

/** Phase 4 — herons, kingfishers, cormorants with wing flap. */
export function Birds(_props: HeroComponentProps = {}) {
  return null;
}
