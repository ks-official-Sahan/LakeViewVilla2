"use client";

import type { HeroComponentProps } from "../types";

/** Phase 6 — height-stratified humidity mist particles. */
export function Mist(_props: HeroComponentProps = {}) {
  return null;
}
