"use client";

import type { HeroComponentProps } from "../types";

/** Phase 4 — curved trunk palms with instanced fronds. */
export function PalmTrees(_props: HeroComponentProps = {}) {
  return null;
}
