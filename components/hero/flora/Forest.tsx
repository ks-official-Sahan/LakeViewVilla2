"use client";

import type { HeroComponentProps } from "../types";

/** Phase 4 — 200+ instanced tropical forest (3 draw calls). */
export function Forest(_props: HeroComponentProps = {}) {
  return null;
}
