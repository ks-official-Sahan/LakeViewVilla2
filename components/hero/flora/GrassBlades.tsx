"use client";

import type { HeroComponentProps } from "../types";

/** Phase 4 — instanced grass with GPU wind shader. */
export function GrassBlades(_props: HeroComponentProps = {}) {
  return null;
}
