"use client";

import type { HeroComponentProps } from "../types";

/** Phase 2 — Gerstner wave lake with tropical water shader. */
export function Lake(_props: HeroComponentProps = {}) {
  return null;
}
