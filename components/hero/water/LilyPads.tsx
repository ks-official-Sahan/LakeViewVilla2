"use client";

import type { HeroComponentProps } from "../types";

/** Phase 2 — instanced lily pads and lotus blooms. */
export function LilyPads(_props: HeroComponentProps = {}) {
  return null;
}
