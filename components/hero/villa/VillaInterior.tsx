"use client";

import type { HeroComponentProps } from "../types";

/** Phase 5 — furniture, lamps, rugs, tropical plants. */
export function VillaInterior(_props: HeroComponentProps = {}) {
  return null;
}
