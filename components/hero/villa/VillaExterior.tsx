"use client";

import type { HeroComponentProps } from "../types";

/** Phase 5 — walls, roof, glass facade, balcony, pillars. */
export function VillaExterior(_props: HeroComponentProps = {}) {
  return null;
}
