"use client";

/**
 * Exterior geometry is assembled in `buildVillaExterior.ts` and mounted via `Villa.tsx`.
 * This module remains the Phase 5 exterior boundary for future declarative migration.
 */
export { buildVillaExterior } from "./buildVillaExterior";
