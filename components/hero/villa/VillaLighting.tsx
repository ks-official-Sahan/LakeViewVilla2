"use client";

import type { HeroComponentProps } from "../types";

/** Phase 5 — interior point lights, sconces, garden lantern. */
export function VillaLighting(_props: HeroComponentProps = {}) {
  return null;
}
