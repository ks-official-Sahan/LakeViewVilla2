"use client";

import type { HeroComponentProps } from "../types";

/** Phase 3 — lake field strip at ~85° CCW from road junction. */
export function LakeField(_props: HeroComponentProps = {}) {
  return null;
}
