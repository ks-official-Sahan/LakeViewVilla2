"use client";

import type { HeroComponentProps } from "../types";

/** Phase 3 — far shore wedge and background terrain. */
export function FarShore(_props: HeroComponentProps = {}) {
  return null;
}
