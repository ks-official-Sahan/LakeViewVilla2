"use client";

import type { HeroComponentProps } from "../types";

/** Phase 3 — sloped embankment between road and lake. */
export function GrassStrip(_props: HeroComponentProps = {}) {
  return null;
}
