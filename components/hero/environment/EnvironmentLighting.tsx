"use client";

import type { HeroComponentProps } from "../types";

/** Phase 1 — time-of-day AmbientLight, HemisphereLight, DirectionalLight. */
export function EnvironmentLighting(_props: HeroComponentProps = {}) {
  return null;
}
