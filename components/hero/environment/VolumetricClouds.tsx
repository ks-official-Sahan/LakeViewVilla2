"use client";

import type { HeroComponentProps } from "../types";

/** Phase 1 — merged cloud clusters or shader volumetric layer. */
export function VolumetricClouds(_props: HeroComponentProps = {}) {
  return null;
}
