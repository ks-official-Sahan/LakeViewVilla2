"use client";

import type { HeroComponentProps } from "../types";

/** Phase 1 — sun/moon orbs and glow sprites. */
export function CelestialBodies(_props: HeroComponentProps = {}) {
  return null;
}
